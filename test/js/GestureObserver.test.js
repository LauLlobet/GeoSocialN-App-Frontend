/*global define, require, module, asyncTest, equal, start, QUnit, setTimeout, notEqual, deepEqual*/
define([], function () {
    'use strict';
    module('Gesture Observer');
/*
MANUAL TEST NOT FOR AUTOMATE TESTING
    asyncTest('Swipe right and left', function () {
        require([ "PhaserGame", "GestureObserver"], function (PhaserGame, GestureObserver) {
            var gestureObserver = new GestureObserver({
                    swipeRight: function () { console.log("swipe right"); },
                    swipeLeft: function () { console.log("swipe left"); }
                }),
                phaserGame = new PhaserGame(function () {
                    equal("ok", "ok", 'manual test, swipe with cursor an watch console');
                    QUnit.start();
                }, gestureObserver);
        });
    });*/
});